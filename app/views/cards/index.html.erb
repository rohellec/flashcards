<h1>Флеш-карточки</h1>

<% if @cards.any? %>

<table class="table table-hover">
  <thead>
    <tr>
      <th class="col-xs-2">Оригинал</th>
      <th class="col-xs-3">Перевод</th>
      <th class="col-xs-3 text-center">Дата пересмотра</th>
      <th class="col-xs-4"></th>
    </tr>
  </thead>
  <tbody>
    <%= render @cards %>
  </tbody>
</table>

<% else %>

<p>Пока не добавлено ни одной карточки...</p>

<% end %>
