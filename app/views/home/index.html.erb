<div class="container-fluid">

  <%= render "shared/flash_messages" %>

  <% if logged_in? %>

    <% if @card %>

      <div class="row">
        <div class="col-md-6 col-md-offset-3 card-form">
          <h1>Флеш-карточка</h1>
          <%= render "/home/form" %>
        </div>
      </div>

    <% elsif current_cards.any? %>

      <div class="well well-sm">
        На сегодня карточки закончились
      </div>

    <% else %>

      <div class="well well-sm">
        Вы пока не добавили ни одной карточки. Добавьте несколько для начала работы
      </div>

    <% end %>

  <% else %>

    <div class="center jumbotron">
      <p>Первый в мире удобный менеджер флеш-карточек. Именно так.</p>
      <%= link_to "Регистрация", signup_path, class: "btn btn-lg btn-success" %>
    </div>

  <% end %>

</div>
